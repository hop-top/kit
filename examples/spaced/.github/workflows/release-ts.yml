name: Release TypeScript
on:
  push:
    tags: ['sdk/sdk/sdk/sdk/sdk/sdk/sdk/sdk/sdk/ts/v*']

jobs:
  publish:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ts
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: sdk/sdk/sdk/sdk/sdk/sdk/sdk/sdk/sdk/ts/.nvmrc
          registry-url: 'https://registry.npmjs.org'
      - uses: pnpm/action-setup@v4
      - run: pnpm install --frozen-lockfile
      - run: pnpm test
      - name: Version from tag
        run: npm version ${GITHUB_REF#refs/tags/sdk/sdk/sdk/sdk/sdk/sdk/sdk/sdk/sdk/ts/v} --no-git-tag-version
      - run: pnpm publish --access public --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
